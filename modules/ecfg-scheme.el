;; -*- lexical-binding: t -*-

(defun ecfg-scheme-module-init ()
  ;; (autoload 'run-scheme "cmuscheme" "Run an inferior Scheme" t)
  ;; (autoload 'scheme-smart-complete "scheme-complete" nil t)
  ;; (setq scheme-program-name "csi -:c")

  ;; (add-hook 'scheme-mode-hook 'ecfg--scheme-hook)
  )


(defun ecfg--scheme-hook ()
  ;; (local-set-key (kbd "<C-tab>") 'scheme-smart-complete)
  ;; (require 'chicken)
)
